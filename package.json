{
  "name": "crypto-analyst-trader",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Risk-gated daily trading system with direct exchange execution",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "format:check": "prettier --write docs/analytics/market-regime-context.md && gzip -c docs/analytics/market-regime-context.md | base64 -w0 && echo && prettier --check .",
    "test": "node --import tsx --test tests/**/*.test.ts",
    "test:release": "npm run typecheck && npm run lint && npm run format:check && npm test"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "eslint": "^10.8.0",
    "prettier": "^3.8.0",
    "tsx": "^4.22.0",
    "typescript": "^5.9.0",
    "typescript-eslint": "^8.0.0"
  }
}
